name: Trigger auto deployment for container-app-qz5x5dc7ne4qm

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-qz5x5dc7ne4qm-AutoDeployTrigger-f9ade51c-fb09-4174-a1ce-f1ab843bcbba.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPQZ5X5DC7NE4QM_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPQZ5X5DC7NE4QM_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPQZ5X5DC7NE4QM_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPQZ5X5DC7NE4QM_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPQZ5X5DC7NE4QM_REGISTRY_PASSWORD }}
          containerAppName: container-app-qz5x5dc7ne4qm
          resourceGroup: dotnetcoreapib66f
          imageToBuild: default/container-app-qz5x5dc7ne4qm:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


